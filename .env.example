# Point this at your Laravel backend's API prefix.
# Local dev (Laravel `php artisan serve` default):
VITE_API_BASE_URL=http://localhost:8000/api

# If frontend and backend are on different domains/servers,
# set this to that domain, e.g.:
# VITE_API_BASE_URL=https://backend.dane.org/api
